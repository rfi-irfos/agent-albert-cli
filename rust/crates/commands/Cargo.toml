[package]
name = "albert-commands"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Slash command library for Albert CLI — /plan, /tdd, /loop, /compress, /code-review, /build-fix, /refactor and more"
readme = "README.md"
keywords = ["ai", "cli", "agent", "commands", "llm"]
categories = ["command-line-utilities", "development-tools"]

[dependencies]
runtime = { package = "albert-runtime", path = "../runtime", version = "1.5.0" }
console = "0.15"

[lints]
workspace = true
